export type TimelineEntryData = {
  title: string;
  imageName: string;
  description: string;
  destination: string;
  icon: React.ElementType;
  anchor: string;
};

export type TimelineLegendData = {
  title: string;
  icon: React.ElementType;
  color: string;
  anchor: string;
};
